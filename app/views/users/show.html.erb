<h1>Your questions: </h1>
<% @user.questions.each do |question| %>
  <ul>
    <li><%= link_to "#{question.title}", question_path(question) %> <%= link_to "Edit", edit_question_path(question.id) %></li>
  </ul>
  <% end %>
<h3>Fill out the form below and watch the answers roll in!</h3>
<%= flash[:errors] %>
<%= form_for :question do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :body %>
<%= f.text_area :body, size: "50x12" %>
<%= f.submit "Ask Them!" %>
<% end %>
